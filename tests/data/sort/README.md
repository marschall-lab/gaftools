Directory contains gaf alignments which are sorted and compared.

reads.fa

* reads used to make the GAF

smallgraph.gaf

* reads.fa aligned to `tests/data/smallgraph.gfa`

smallgraph.gaf.gz

* bgzipped gaf
