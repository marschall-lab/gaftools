Directory contains reads to check stat information

reads.fa

* reads used for alignment.

graphaligner.gaf(.gz)

* alignment to `tests/data/smallgraph.gfa` using GraphAligner

minigraph-unstable.gaf(.gz)

* alignment to `tests/data/smallgraph.gfa` using minigraph with `--vc`

minigraph-stable.gaf(.gz)

* alignment to `tests/data/smallgraph.gfa` using minigraph without `--vc`
